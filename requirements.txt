# Runtime dependencies for the simplified student demo (Render deployment).
# Versions pinned from poetry.lock. ML/notebook/dev tooling is intentionally
# excluded (jupyter, streamlit, certbot, pandoc, xgboost, pyreadstat, black, ...)
# as it is not used at request time. scikit-learn is pinned to 1.5.0 to stay
# compatible with the pickled model app/static/data/model_v2.pkl.
flask==3.0.3
flask-sqlalchemy==3.1.1
flask-login==0.6.3
flask-wtf==1.2.1
flask-migrate==4.0.7
flask-bootstrap==3.3.7.1
werkzeug==3.0.3
python-dotenv==1.0.1
pandas==2.2.2
numpy==1.26.4
scipy==1.13.1
scikit-learn==1.5.0
matplotlib==3.9.0
gunicorn==22.0.0
